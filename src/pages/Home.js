import React from 'react';
import WeatherCard from '../components/weatherCard';

const Home = () => {
  return (
    <div className="p-4">
      <WeatherCard />
    </div>
  );
};

export default Home;
